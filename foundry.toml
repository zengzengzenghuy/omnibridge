[profile.default]
src = "contracts"
test = "foundry-tests"
out = "out"
libs = ["lib"]
cache_path = "forge-cache"

solc = "0.7.5"
evm_version = "istanbul"
optimizer = true
optimizer_runs = 200

# Existing JS/Truffle tests stay under test/; Foundry tests live in foundry-tests/.
